<policies>
    <inbound>
        <base />
        <include-fragment fragment-id="mcp-jwt-auth" />
    </inbound>
    <backend>
        <forward-request timeout="300" buffer-response="false" />
    </backend>
    <outbound>
        <base />
        <choose>
            <when condition="@(context.Response != null &amp;&amp; context.Response.StatusCode == 401)">
                <set-header name="WWW-Authenticate" exists-action="override">
                    <value>Bearer realm="mcp", resource_metadata="{{APIMGatewayURL}}/ms-purview-mcp/.well-known/oauth-protected-resource"</value>
                </set-header>
            </when>
        </choose>
    </outbound>
    <on-error>
        <base />
        <choose>
            <when condition="@(context.LastError != null &amp;&amp; context.LastError.Source == &quot;validate-jwt&quot;)">
                <return-response>
                    <set-status code="401" reason="Unauthorized" />
                    <set-header name="WWW-Authenticate" exists-action="override">
                        <value>Bearer realm="mcp", resource_metadata="{{APIMGatewayURL}}/ms-purview-mcp/.well-known/oauth-protected-resource"</value>
                    </set-header>
                </return-response>
            </when>
        </choose>
    </on-error>
</policies>
